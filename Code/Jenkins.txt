def pompath = "Code"
pipeline {
    agent any 
    environment {
    PATH = "C:\\WINDOWS\\SYSTEM32"
}
    stages {
        stage('Checkout from SCM ') { 
            steps {
               git credentialsId: 'GitHub_Credential_raghav', url: 'https://github.com/jha-raghav/HappyTrip.git'
            }
        }
		stage('Build') {
			 tools {
	           jdk 'JDK8'
	           maven 'Maven'
	 }
		  steps {
		       bat 'start cmd.exe /c mvn -package'
		   }
		}
       
    }
}
